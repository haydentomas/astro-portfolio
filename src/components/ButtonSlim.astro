---
const { href } = Astro.props;
const { aria } = Astro.props;
const { target } = Astro.props;
---

<a class="button" aria-label={aria} href={href} target={target}>
<button class="btn draw-border hidden md:inline-flex">
    <slot />
</button></a>

  <!-- <style>
   .draw-border {
  box-shadow: inset 0 0 0 2px #969696;
@apply dark:text-white uppercase;
  transition: color 0.25s 0.0833333333s;
  position: relative;
  border-radius:10px;
  overflow: hidden;
}
.draw-border::before, .draw-border::after {
  border: 0 solid transparent;
  box-sizing: border-box;
  content: "";
  pointer-events: none;
  position: absolute;
  width: 0;
  height: 0;
  bottom: 0;
  right: 0;
  border-radius: inherit;
  clip-path: inset(0 0 0 2px) circle(50% at 50%);
  transition: clip-path 0.65s, border-color 0.3s ease-in-out;
}
.draw-border::before {
  border-bottom-width: 2px;
  border-left-width: 2px;
}
.draw-border::after {
  border-top-width: 2px;
  border-right-width: 2px;
}
.draw-border:hover {
  
}
.draw-border:hover::before, .draw-border:hover::after {
    @apply border-teal-400;

  transition: border-color 0s, width 0.65s, height 0.65s;
  width: 100%;
  height: 100%;
  clip-path: inset(0); /* Remove clip on hover to reveal full border */
}
.draw-border:hover::before {
  transition-delay: 0s, 0s, 0.65s;
}
.draw-border:hover::after {
  transition-delay: 0s, 0.65s, 0s;
}

.btn {
  background: none;
  border: none;
  cursor: pointer;
  line-height: 1.5;
  padding: 0.4em 1em;
  letter-spacing: 0.05rem;

}
.btn:focus {
  outline: 1px dotted #1e1f1f;
}

  </style> -->


  <style>
    a.button {
  position: relative;
padding:5px 20px;
  display:inline-block;


  letter-spacing: 2px;
  text-decoration: none;
  text-transform: uppercase;
  text-align: center;
  color: #fff;
  transition: 0.5s;
  border: 1px solid #fff;
  
  
  button {
  background:transparent;
  border:none;
  color:#fff;
}
  
  &:hover {
     border: 1px solid transparent;
background: var(--color-purple) url(/images/pixel2.png);
  transition-delay: 0.8s;
  background-size: 180px;
  -webkit-animation: animate var(--speed-fast) steps(8) forwards;
          animation: animate var(--speed-fast) steps(8) forwards;
  }

}


@-webkit-keyframes animate {
  0% {
    background-position-y: 0;
  }
  100% {
    background-position-y: -480px;
  }
}

@keyframes animate {
  0% {
    background-position-y: 0;
  }
  100% {
    background-position-y: -480px;
  }
}
  </style>